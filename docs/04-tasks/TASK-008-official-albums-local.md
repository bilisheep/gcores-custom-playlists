# TASK-008: 官方播单页集成本地播单

- 状态：完成
- 目标与范围：在官方 `/albums` 页面首位增加“我的”分类并默认展示本地播单；本地卡片与详情复用机核页面结构和视觉；保留本地封面的设置、更换、移除及既有 CRUD、播放、排序、分享能力。
- 非范围：新增第二个左栏入口、播单管理弹窗、写入机核账号、封面分享/图床、修改官方播单或分页、图片裁剪编辑器。
- 关联需求、设计与 ADR：REQ-CATALOG-001；`docs/01-design/project-design.md`；无 ADR。
- 实施步骤：保留 cover 数据与原生压缩；移除 TASK-007 的弹窗和额外左栏入口；注入“我的”页签；实现 `/albums` 本地库与 `?gcpl=` 详情路由；复用现有 CRUD/播放/排序/分享；保证官方 filter 恢复；真实 SPA、刷新、返回、多标签与截图验证；更新 README 和版本。
- 测试与验收：AC-CATALOG-001；语法、自检、旧状态迁移、合法/无效/超大封面、设置/替换/移除、自动封面、默认“我的”、官方 filter 无回归、库/详情/返回/刷新、无效 ID、跨标签同步、二维码载荷、键盘和减少动态效果。
- 验证结果或阻塞/取消原因：真实 `/albums` 验证“我的 / 机核 / 入驻”顺序且“我的”默认 active；本地库为官方 2/3/4/6 列网格，卡片进入 `/albums?gcpl=` 完整详情且无弹窗；浏览器返回、刷新、无效 ID 回退和官方列表/分页恢复通过。封面原图经原生文件输入、Image/Canvas 处理为 320×320 JPEG；非图片和 9 MiB 文件零写入；设置、更换、移除、首期自动封面及远端 GM 同步通过；二维码载荷仍只有 `v/n/i`。立即注入复现 React hydration 错误后增加 1.5 秒保护，复验无错误。Node 语法、自检、diff、Markdown 链接和独立审计通过；审计无高、中遗留。
