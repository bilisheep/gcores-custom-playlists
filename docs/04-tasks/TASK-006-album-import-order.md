# TASK-006: 专辑一键加入与正倒序播放

- 状态：完成
- 目标与范围：在音频专辑页增加全部节目一键加入；为每个播单增加正序/倒序播放方向，并让渲染、移动、播放控制、自然播完和分享一致。
- 非范围：视频专辑、按发布时间/章节号重新计算混合播单顺序、改变付费权限、随机播放。
- 关联需求、设计与 ADR：REQ-ALBUM-001、REQ-ORDER-001；`docs/01-design/project-design.md`；无 ADR。
- 实施步骤：扩展播单 direction；集中 orderedItems；更新所有顺序消费者；实现专辑详情/内容分页；挂载按钮与状态；真实专辑和播放顺序验证；更新 README 和版本。
- 测试与验收：专辑230首次10期/二次0期；跨页专辑完整；分页失败原子性；A/B/C 正倒序；切换时断点不变；倒序上下移动；分享可见顺序；语法、自检和审计。
- 验证结果或阻塞/取消原因：真实专辑 230 首次加入 10 期、二次加入 0 期；专辑 342 完整加入 27 期，首尾 ID 为 `217132` / `211278`；模拟第二页 HTTP 500 时写入 0 期；`content-type=mixed` 的专辑 306 不显示按钮。A/B/C 正序与 C/B/A 倒序、切换时 B/42 秒断点不变、倒序移动和下一期均验证通过；语法、自检、diff 检查和独立审计通过。
