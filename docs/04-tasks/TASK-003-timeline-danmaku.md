# TASK-003: 时间轴时刻评论弹幕

- 状态：完成
- 目标与范围：把机核评论接口返回的时刻评论按 `radio-timestamp` 同步显示在官方全屏时间轴区域；实现分页、4 轨移动、暂停/继续、seek 重置、本地开关和减少动态效果。
- 非范围：自定义底部播放器弹幕、发表评论、弹幕样式配置、服务端或长期评论缓存。
- 关联需求、设计与 ADR：REQ-DANMAKU-001；`docs/01-design/project-design.md`；无 ADR。
- 实施步骤：实现评论分页和正文清洗；挂载时间轴弹幕层；同步官方音频事件；实现轨道和动画；实现开关与可访问性；真实页面验证；更新 README、版本与状态。
- 测试与验收：`node --check`、内置自检、结构化评论 API、真实时间轴触发/暂停/seek/开关、DOM 不遮挡、减少动态效果静态显示。
- 验证结果或阻塞/取消原因：真实节目 218100 加载 98 条结构化时刻评论；75 秒评论按时显示并移除时间前缀；暂停后动画 `currentTime` 在 900ms 内保持不变；seek 到 200 秒立即清空且不补发；向后 seek 再次经过 75 秒可重播；开关写入本地状态并清空/恢复；减少动态效果只生成 opacity 关键帧；退出全屏后层与开关均卸载；模拟 React 移除弹幕 root 后自动重挂；截图 `docs/screenshots/timeline-danmaku.jpg` 为 1280×720 JPEG。Node 语法、自检、Markdown 链接与独立审计通过。
