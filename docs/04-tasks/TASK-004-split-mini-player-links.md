# TASK-004: 拆分底部播放器封面与标题行为

- 状态：完成
- 目标与范围：把底部播放器左侧从单一点击区域拆分为封面和标题两个明确入口；封面进入当前/断点节目详情，标题打开自定义播单管理面板。
- 非范围：修改播放、断点、音量、弹幕、播单数据或节目详情页内容。
- 关联需求、设计与 ADR：REQ-PLAYER-001；`docs/01-design/project-design.md`；无 ADR。
- 实施步骤：拆分 mini DOM；增加节目详情链接；保留标题管理按钮；调整 focus/hover/响应式样式；真实页面验证两个入口；更新 README 和版本。
- 测试与验收：`node --check`、内置自检、封面链接目标、标题打开面板、空播单行为、键盘焦点、现有播放控制回归。
- 验证结果或阻塞/取消原因：真实页面验证封面链接为 `/radios/218527`、`target=_blank`、`rel=noopener noreferrer` 且新标签成功打开对应节目详情；标题按钮保持原 URL 并打开管理面板；空播单无详情链接但保留标题管理按钮；ARIA 标签、键盘焦点、42px 移动端封面、减少动态效果和节目 ID 输入校验通过。Node 语法、自检、Markdown 链接、diff 检查与独立审计通过。
