# 项目定义

## 项目背景与待解决问题

机核网页端原生播放队列只提供单一临时列表，难以管理多个主题播单、可靠恢复列表级播放断点或跨用户分享播单。

## 项目目标

提供无需服务器、安装后即可使用的 Tampermonkey 用户脚本和 Chrome/Edge 扩展，让机核网页用户拥有独立的多播单、连续播放、断点续播、批量收集与分享能力。

## 目标用户与核心价值

- 目标用户：主要在桌面浏览器收听机核播客、希望长期整理节目的人。
- 核心价值：在不修改机核账号数据的前提下，以本地存储获得稳定、可迁移的播单体验。

## 本期范围

- 多播单及节目增删改查和排序。
- 官方播单页面中的“我的”本地播单分类，以及播单自定义封面。
- 独立播放器、音量和列表级断点。
- 用户参与节目全分页批量加入。
- 二维码和链接分享、确认导入。
- 官方全屏时间轴中的时刻评论弹幕。
- 普通与授权音频播放、SPA 页面适配、多标签页同步。
- Chrome/Edge 共用的 Manifest V3 扩展 ZIP，以及 GitHub Release 发布。

## 非项目范围

- 机核账号侧或服务器侧播单同步。
- 绕过会员、付费、登录或内容访问限制。
- 移动端机核 App 集成。
- 自建后端、数据库、构建系统或浏览器扩展商店发布。
- 自定义封面随二维码分享、上传图床或账号同步。
- Chrome Web Store、Edge Add-ons、Firefox 或 Safari 扩展发布。
- Tampermonkey 与扩展之间的数据迁移或共存运行。

## 关键约束

- 业务逻辑只维护在单文件用户脚本；扩展构建层只负责存储桥和打包，无运行时服务、框架、第三方构建依赖或 API Key。
- 数据按分发方式保存在 Tampermonkey `GM_*` 或扩展 `chrome.storage.local`，均不上传服务器。
- 依赖机核网页结构和当前 `/gapi/v1` 接口行为。
- 分享二维码不包含账号、进度和音频授权地址。
- 自定义封面仅保存在当前分发方式的浏览器本地存储，经过浏览器端裁切压缩后写入。
- 扩展使用独立的 `chrome.storage.local` 空存储，首次安装不读取或迁移 Tampermonkey 数据；油猴版与扩展版应二选一启用。

## 阶段定义与目标里程碑

1. 核心播放与管理：已完成。
2. 分享、批量加入和多标签同步：已完成。
3. 公开仓库、文档与截图：本任务完成后进入维护阶段。
4. 时间轴弹幕：已完成，进入维护阶段。
5. 官方播单页本地分类与封面：已完成，进入维护阶段。
6. Chrome/Edge 扩展与 GitHub Release：已完成，进入维护阶段。

## 成功标准

- 用户能安装脚本并在机核官方播单页面优先浏览自己的本地播单、设置封面，并完成添加、管理、播放、断点恢复、批量加入和分享导入。
- 无需额外依赖即可通过语法检查和内置自检。
- README 能独立说明安装、配置、使用、隐私和限制。
- 同一扩展 ZIP 可在 Chrome 和 Edge 开发者模式加载，且 Release 资产、哈希和安装说明可验证。

## 已确认决定与开放问题

- 已确认：GitHub 公开仓库，项目自身采用 MIT 许可证。
- 已确认：项目名与仓库名为 `gcores-custom-playlists`。
- 已确认：本地播单集成到官方 `/albums` 页面，“我的”位于“机核 / 入驻”之前并默认展示；封面仅保存在本机，不进入二维码或机核账号。
- 已确认：发布 GitHub Release 的 Chromium MV3 ZIP，不提交浏览器商店；扩展首次安装为空存储，不迁移 Tampermonkey 数据。
- 开放问题：是否添加自动更新元数据、分片二维码或跨设备同步；当前均不阻塞。
