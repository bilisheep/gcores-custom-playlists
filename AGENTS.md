# 项目协作规则

## 项目文档工作流

### 开始任务前

1. 阅读本文件、`PROJECT.md` 和 `STATUS.md`。
2. 按下方矩阵中可判定的触发条件继续阅读对应文档。
3. 核对仓库实际状态；不得只依据单份任务描述修改代码。
4. 若目标、范围、行为、接口、安全、兼容性或验收存在会改变结果的歧义，停止修改并向用户确认。

### 文档参考与更新矩阵

| 文档 | 何时必须参考 | 何时必须更新 | 如何更新 |
| --- | --- | --- | --- |
| `README.md` | 处理安装、配置、使用、截图、测试、发布入口时 | 能力、前置条件、配置、命令、截图或入口链接变化 | 只保留当前入口，实际验证命令；项目进度只链接 `STATUS.md` |
| `AGENTS.md` | 每次任务开始 | 文档路径、职责、阅读规则或质量门变化 | 增量更新本矩阵并清理失效引用，不复制需求正文 |
| `PROJECT.md` | 判断目标、范围、非范围、约束或成功标准时 | 这些基线内容经用户确认发生变化 | 更新当前定义，并同步 PRD、验收和状态 |
| `docs/00-prd/PRD.md` | 修改用户可见行为、业务规则、异常、权限或版本范围前 | 任一需求行为或状态变化 | 保留稳定 `REQ-*` 编号，先更新需求再同步设计和验收 |
| `docs/01-design/project-design.md` | 修改系统组成、数据流、播放状态、接口协作或项目不变量前 | 对应设计事实变化 | 更新当前设计并引用受影响的需求和验收项 |
| `docs/02-acceptance/acceptance-criteria.md` | 规划实现、测试或判断完成前 | 需求、非功能要求或验收基线变化，且必须在实现/验收前 | 更新可观察动作、预期结果和证据要求，不为通过失败结果事后放宽 |
| `docs/04-tasks/TASK-*.md` | 开始或继续一次可交付修改前 | 任务范围、状态、阻塞或验证结果变化 | 按 `计划 → 进行中 → 完成` 更新；完成必须写明验证 |
| `STATUS.md` | 每次任务开始和结束 | 阶段、进展、阻塞、风险、下一步或最近验证变化 | 只保留当前快照，不累积历史 |
| `THIRD_PARTY_NOTICES.md` | 修改、替换或新增内嵌第三方代码时 | 第三方名称、版本、来源或许可证变化 | 保留完整归属与许可证正文，并同步 README |
| `LICENSE` | 修改项目授权、发布或分发规则时 | 仅在用户明确确认新的项目许可证后 | 更新当前许可证并检查 README、第三方声明和远端发布信息 |

### 文档生命周期

- README、AGENTS、PROJECT、设计和 STATUS 是生活文档，只保留当前有效内容，历史交给 Git。
- 需求状态使用 `草拟 → 已确认 → 已实现 → 已验收`；延期、取消或替代必须先确认影响，稳定编号不得复用。
- 任务状态使用 `计划 → 进行中 → 完成`，也可转为阻塞或取消；完成必须记录验证结果，取消必须记录原因。
- 合并、移动、替代或退役文档前先确认，并清理 README、AGENTS 和下游文档中的全部失效引用。

### 基线与受控变更

- `PROJECT.md` 与 PRD 当前内容是已确认基线。
- 目标、范围、可见行为、数据、接口、安全、兼容性或验收发生实质变化时，先说明影响并获得确认，再更新权威文档、实现和下游引用。
- 文档与代码不一致时，把两者视为冲突证据，停止扩大修改并向用户确认预期。

### 代码与资源约束

- 项目保持单文件用户脚本；没有明确需求时不添加构建系统、框架或运行时服务。
- 不绕过机核登录、会员、付费或内容访问权限。
- 不提交 Cookie、Token、授权 URL、账号信息或带登录头像的截图。
- 内嵌二维码库是第三方生成代码；除非升级依赖，不直接手工修改其 minified 内容。

### 完成任务前

1. 运行与风险相称的语法、自检和真实页面验证。
2. 核对实现、需求、设计、README 和验收标准一致。
3. 更新受影响的任务、状态和第三方许可文档。
4. 检查 Markdown 链接和截图路径真实存在，Git 工作区只包含预期修改。
5. 报告已完成内容、验证结果、审计结论以及仍存在的风险。

